import { SvgloaderDirective } from './svgloader.directive';

describe('SvgloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgloaderDirective();
    expect(directive).toBeTruthy();
  });
});
